MERN app deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components:
{{- range $name := list "backend" "frontend" "mongodb" }}
{{- $config := index $.Values $name }}
{{- if and $config $config.enabled }}
  - {{ $name }}: {{ $config.image.repository }}:{{ $config.image.tag }}
{{- end }}
{{- end }}

Check rollout:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if and .Values.ingress .Values.ingress.enabled }}

Access the app (via ingress):
  http{{ if .Values.ingress.tlsSecret }}s{{ end }}://{{ .Values.ingress.host }}/
  API health: http{{ if .Values.ingress.tlsSecret }}s{{ end }}://{{ .Values.ingress.host }}/api/v1/health

If "{{ .Values.ingress.host }}" is not in DNS, map it in your hosts file to the
ingress controller's node IP.
{{- end }}

Quick API smoke test without ingress:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "mern-app.fullname" . }}-backend 4000:80
  curl http://localhost:4000/api/v1/health
